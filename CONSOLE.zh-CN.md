# Router Studio 控制台

[English](CONSOLE.md) | **简体中文**

本地运行的双语策略编辑器。右上角 **中 / EN** 切换整个界面，记住语言选择，保留当前草稿。无需 npm、API Key 或第三方 Python 包，使用 Python 3.10+。

## 启动

在仓库目录运行：

```sh
python run_console.py --open
```

默认编辑仓库内的示例策略，**不会修改已安装 Skill**。关闭运行 Python 的终端即可停止服务。

先按 [部署说明](README.zh-CN.md) 安装或升级到 1.3，然后编辑实际安装的 Skill：

```sh
python run_console.py --installed --open
```

安装目录取 `CODEX_HOME`，未设置则取 `~/.codex`。升级之前请备份自己的模型选择与项目说明；已有用户应保留自己的 `references/workspaces.md`，不要直接用公共模板覆盖。

也可以指定一个配置文件：

```sh
python run_console.py --policy /absolute/path/policy.json --port 0 --open
```

`--port 0` 自动选择空闲端口。每次启动生成临时本地访问令牌，终端输出的完整网址可以打开控制台；不要把该网址分享给别人。备份默认存于仓库的 `.local/console/history`，可用 `--data-dir` 更改。

## 怎么用

1. **策略总览**：开关三种分流条件，设置同时运行数、累计启动数、工作量门槛及恢复上限。
2. **模型分档**：修改四个分档的模型 ID、推理强度与回退顺序；支持自定义模型 ID。
3. **模拟实验室**：选择任务场景及资源环境，查看真实规则引擎的建议和原因。它使用合成输入，假定候选支持界面列出的强度，不查询账户、不调用模型、不实际派发代理。
4. **应用更改**：检查差异后保存。旧配置自动备份；如果文件被外部程序更新，会阻止覆盖并提示重新载入。
5. **版本记录**：将旧配置载入草稿，再确认应用以恢复。导入与预设也只修改草稿。

修改只影响之后重新读取此策略的决策，已派发任务不会改变。三个分流条件都是**许可条件**：开启不等于强制派发；主机、范围、资源冲突与预算检查仍然执行。关闭独立复核开关，会让相应场景留在主任务处理。

## 本地项目和 SSH

每个服务实例只编辑启动时指定的一个文件，页面显示完整目标路径。不会自动扫描项目、连接 SSH 或同步到其他主机。要编辑远端策略，可以把仓库放在远端运行，使用 SSH 本地端口转发访问其回环地址；不要将服务改为公网监听。远端需要自己的 Python 与 Skill 安装。

## 范围与限制

- 这是子代理策略控制台，不能自动切换当前对话的主模型。
- 使用流动路径、开关与页面过渡动画，支持系统“减少动态效果”设置。
- 服务仅监听 `127.0.0.1`，验证访问令牌、Host 与 Origin，不提供任意文件读写接口。
- 本地历史、草稿导出及目标路径可能包含个人配置，默认不提交到 Git。
- 不自动修改规则、自更新、开机启动或发送遥测。浏览器草稿仅在当前页面内存中；切换语言保留，刷新或关闭前请保存或导出。
- 原子替换与版本检查能防止通常的旧版本覆盖，但不应让多个程序同时写同一个策略文件。

运行验证：

```sh
python -B -m unittest console.test_console -v
python -B -m unittest discover -s workspace-router/tests -v
```

## 推荐默认（1.3）

点击 **恢复推荐默认**，同时重置触发条件、协作限额、分档方式和全部模型候选。先载入草稿，再确认应用。模型页面解释适用边界，模拟实验室显示命中原因。详见 [任务分界](workspace-router/references/task-boundaries.zh-CN.md)。导入旧配置会明确显示旧版分档，不会静默改用新规则。

## 三套完整预设

经济 / 稳定 / 土豪，默认稳定。预设包含完整模型候选与边界策略；选择后先模拟，再确认应用。详见[严格条件与官方、用户实测依据](workspace-router/references/presets.zh-CN.md)。三套都不切换主模型，也不承诺省订阅额度。
